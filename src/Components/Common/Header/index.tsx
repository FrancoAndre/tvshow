import {
    ContainerHeader,
    TitleHeader,
} from './styles';

export const Header = () => {
    return (
        <ContainerHeader>
            <TitleHeader>TV SHOW</TitleHeader>
        </ContainerHeader>
    )
}